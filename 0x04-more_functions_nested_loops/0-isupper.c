#include "main.h"
/**
 * _issupper - checks for uppercase
 * @c: variable
 * Return: 1 if funct works else return 0
*/
int _isupper(int c)
{
if (c == 'A' && c <= 'Z')
{
return ('1');
}
else
{
return ('0');
}
}
